apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: default
  labels:
    app: microservicio-employee-service
    role: backend
    stage: production
spec: 
  ports:
    - name: http
      port: {{.values.servicePort}}
  selector:
    app: microservicio-employee-service
    version: v1.0.0
  type: NodePort
